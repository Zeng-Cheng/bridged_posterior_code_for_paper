linters: linters_with_defaults(
    commented_code_linter = NULL,
    quotes_linter = NULL,
    object_name_linter(
      styles = c("snake_case", "symbols", "camelCase", "lowercase", "UPPERCASE"),
      regexes = character()),
    assignment_linter = NULL,
    indentation_linter(indent = 4L, hanging_indent_style = c("never")),
    infix_spaces_linter(exclude_operators = "="),
    seq_linter = NULL
  ) # lintr
encoding: "UTF-8"
